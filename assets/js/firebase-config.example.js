// Firebase project configuration — TEMPLATE.
//
// Copy this file to firebase-config.js (same folder) and replace the
// placeholder values with your real Firebase project config from
// Firebase Console > Project Settings > General > Your apps.
//
// firebase-config.js is gitignored and must never be committed — only
// this example file is tracked in git.

let firebaseConfig = {
  apiKey: "YOUR_API_KEY",

  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",

  projectId: "YOUR_PROJECT_ID",

  storageBucket: "YOUR_PROJECT_ID.firebasestorage.app",

  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",

  appId: "YOUR_APP_ID",
};
